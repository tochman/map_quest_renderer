{
  "name": "map-animation",
  "version": "1.0.0",
  "description": "Indiana Jones style map animation with Puppeteer",
  "main": "animate.js",
  "type": "module",
  "scripts": {
    "animate": "node animate.js",
    "frames": "node animate-frames.js",
    "smooth": "ffmpeg -y -i map-animation.mp4 -vf 'minterpolate=fps=60:mi_mode=mci:mc_mode=aobmc:me_mode=bidir:vsbmc=1' -c:v libx264 -pix_fmt yuv420p -preset slow -crf 18 -movflags +faststart map-animation-smooth.mp4",
    "oldfilm": "ffmpeg -y -i map-animation.mp4 -vf 'noise=alls=20:allf=t+u,colorchannelmixer=.393:.769:.189:0:.349:.686:.168:0:.272:.534:.131,eq=contrast=1.1:brightness=0.05:saturation=0.8,curves=vintage' -c:v libx264 -pix_fmt yuv420p -preset slow -crf 18 -movflags +faststart map-animation-oldfilm.mp4"
  },
  "keywords": [
    "puppeteer",
    "map",
    "animation"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "puppeteer": "^24.34.0"
  }
}
